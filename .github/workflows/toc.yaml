name: TOC Generator

on:
  push:
      paths:
        - 'README.md'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for thesis evaluation."
#  toc:
#    permissions:
#      contents: write
#    uses: Cysharp/Actions/.github/workflows/toc-generator.yaml@main
#    with:
#      TOC_TITLE: "## Table of Contents"
#    secrets: inherit
